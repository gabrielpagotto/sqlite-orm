name: sqlite_orm_example
description: A sample command-line application.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  build_runner: ^2.3.3
  sqlite_orm_annotation:
    path: ../sqlite_orm_annotation

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0

  sqlite_orm_generator:
    path: ../sqlite_orm_generator
